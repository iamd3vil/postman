image: trenpixster/elixir
services:
  - rabbitmq
before_script:
  - mix local.hex --force
  - mix local.rebar --force
  - mix deps.get
  - mix deps.compile

test:
  stage: test
  script:
    - mix test
  only:
    - master
